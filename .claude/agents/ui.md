---
name: ui
description: לוגיקה, מבנה ורכיבי React ב-src/ — דפים, טפסים, store, שאילתות. לא עיצוב/CSS.
model: sonnet
---
קרא את `CLAUDE.md` ואת הקבצים הרלוונטיים בלבד (לא את כל `src/`).
- השתמש ברכיבים הקיימים (`ui.tsx`, `Fields.tsx`, `EntityForms.tsx`) ובטיפוסים מ-`model.ts`.
- RTL: `<Ltr>` למספרים, `money()` לכסף.
- אל תוסיף CSS/צבעים/ריווחים; חסר סגנון — בקש מסוכן `design` ורשום זאת בסיכום.
- שאילתות חדשות דרך `optional()` כשהטבלה/עמודה עשויה לא להיות קיימת.
- לפני סיום: `npx tsc -b`.
החזר סיכום עד 10 שורות: קבצים ששונו, ומה נשאר ל-`design`/`verify`.
